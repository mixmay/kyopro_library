#include <iostream>
#include <string>
#include <vector>
#include <queue>
#include <stack>
#include <set>
#include <cctype>
#include <cmath>
#include<deque>
#include<map>
#include<algorithm>
#include<iomanip>
#include<numeric>
#include<list>
using namespace std;
 
#define rep(i,n) for(int i = 0; i < n; i++)
#define rep1(i,begin,end) for(int i = begin; i < end; i++)
#define per(i,n) for(int i = n-1; i >= 0; i--)
#define all(c) c.begin(), c.end()
#define ll long long
#define INF 2e9
#define LINF 1e18
#define cout(n) cout << fixed << setprecision(n)
#define pii pair<int,int>
#define pll pair<long long,long long>
#define mod 1000000007LL;
#define vi vector<int>
#define vvi vector<vector<int>>
#define vvl vector<vector<ll>>
#define vl vector<ll>
#define vb vector<bool>
#define vvb vector<vector<bool>>

#define ctoi(c) c - '0'
#define itoc(i) '0'+i
#define ctos(c) {c}
#define AlphaToIdx(c) c - 'a'
#define IdxToAlpha(index) 'a'+index

template <typename T>bool chmax(T& a, const T& b) { if (a < b) { a = b; return true; }return false; }
template <typename T>bool chmin(T& a, const T& b) { if (a > b) { a = b; return true; }return false; }
long long Factorial(int n) { ll sum = 1; for (int i = n; i > 0; i--) { sum *= i;  sum %= mod; }return sum; }
long long NC2(int n) { ll ans = (ll)n / 2 * (n - 1LL); return ans; }
bool is_prime(long long n) { for (long long i = 2; i * i <= n; i++) { if (n % i == 0) return false; }return true; }
template <class H>void debug(H&& h) { cout << h << endl;; }
template <class H, class... Ts>void debug(H&& h, Ts&&... ts) { cout << h << " "; debug(forward<Ts>(ts)...); }
template<typename T> void debugdp(vector<vector<T>> vec) {
    ll n = vec.size(); ll m = vec[0].size(); rep(i, n) {
        rep(j, m) { cout << vec[i][j] << " "; }cout << endl;
    }
}
template<typename T> void debugvec(T vec) { int n = vec.size(); rep(i, n)cout << vec[i] << " "; cout << endl; }
template<typename T> using min_priority_queue = priority_queue<T, vector<T>, greater<T>>;
 
//-----------------------------------------------------------------------------------------------------------------
int main() {
}